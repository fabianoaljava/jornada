/*! ========================================================================
 * home.js
 * Page/renders: forntend/index.html
 * Plugins used: carousel
 * ======================================================================== */
$(function () {
    // Carousel
    // ================================
    $("#lovely-client").owlCarousel({
        autoPlay: true,
        autoHeight : true,
        pagination : false
    });
});